/**
 * Test Data Builders - Barrel Export
 * Provides fluent API for creating test data
 */

const AppointmentBuilder = require('./AppointmentBuilder');
const AdminBuilder = require('./AdminBuilder');

module.exports = {
  AppointmentBuilder,
  AdminBuilder,
};
