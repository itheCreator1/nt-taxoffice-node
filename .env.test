# Test Environment Variables
# Used for automated testing

# Server Configuration
PORT=3001
NODE_ENV=test

# Session Secret (test only - not for production)
SESSION_SECRET=test_secret_key_for_testing_only_do_not_use_in_production_12345

# MySQL Test Database Configuration (using Docker Compose MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=nt_taxoffice_test

# Email Configuration (Mock/Test)
GMAIL_USER=test@example.com
GMAIL_APP_PASSWORD=test_app_password

# Admin Email
ADMIN_EMAIL=admin@example.com

# Application URLs
APP_URL=http://localhost:3001
APP_NAME=NT - TAXOFFICE (TEST)

# Rate Limiting (relaxed for testing)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Timezone
TIMEZONE=Europe/Athens

# Appointment Configuration
DEFAULT_SLOT_DURATION=30

# Security
BCRYPT_ROUNDS=4
